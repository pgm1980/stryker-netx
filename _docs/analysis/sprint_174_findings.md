# 360°-Analyse — Sprint 174: Mutations-Pipeline (Findings-Register)

> **Programm:** Sprints 173–178, Findings-only (Issue #276). Status-Schema und Severity
> wie Sprint 173 (`sprint_173_findings.md`): `VERDACHT` / `BESTÄTIGT` / `ENTKRÄFTET` /
> `NOTIZ`; P0–P3.
>
> **Pflicht-Schwerpunkte (aus Sprint-173-Register):**
> 1. **F-14:** `BaseFunctionOrchestrator.cs:117–142` — AddEndingReturn läuft im
>    No-Mutations-Pfad, im Mutations-Pfad hängt alles an `context.InjectMutations(...,
>    !returnType.IsVoid())`; Probe bewies CS0161-CEs für Block-Mutanten. Wirkkette klären.
> 2. **F-10:** Hosting von Deklarations-Level-Mutationen (MethodBodyReplacement 1/1 CE,
>    GenericConstraint) — wie platziert/instrumentiert der Placer Member-Replacements?
> 3. **#277-Folge:** `CsharpMutantOrchestrator.GenerateMutationsForNode:223` ohne
>    try/catch — Robustheits-Frage systematisch (auch Filter-Pipeline, Visitor).
> 4. **F-08:** Equivalence-Pipeline — Erweiterbarkeit Richtung Operator-/Flow-Typfehler.
> 5. **F-25:** Fängt ConservativeDefaultsEquality No-op-Returns (`return 0;`→`return 0;`)?

## Abdeckungs-Protokoll

| Batch | Dateien | Status |
|-------|---------|--------|
| 1 | MutationContext.cs, Instrumentation/EndingReturnEngine.cs (+ BaseFunctionOrchestrator aus 173 vorgelesen) | ✅ gelesen |
| 2 | MutationStore.cs, IfInstrumentationEngine.cs, ConditionalInstrumentationEngine.cs, MutationControl.cs, MemberDefinitionOrchestrator.cs, NodeSpecificOrchestrator.cs, RoslynHelper.TryInjectMutation (Auszug) | ✅ gelesen |
| 3 | MutantPlacer.cs, CsharpMutantOrchestrator.cs (komplett), Mutant.cs, MutantInfo.cs, MutatorProfileFilter.cs, BaseMutantOrchestrator.cs | ✅ gelesen |
| 4 | CommentParser.cs, BlockOrchestrator.cs, StaticConstructorOrchestrator.cs, MutateAtStatementLevelOrchestrator.cs, StatementSpecificOrchestrator.cs, ExpressionSpecificOrchestrator.cs, LocalDeclarationOrchestrator.cs, MemberAccessExpressionOrchestrator.cs, MemberAccessNameSlotOrchestrator.cs, InvocationExpressionOrchestrator.cs, RoslynHelper.ContainsDeclarations (Auszug) | ✅ gelesen |
| 5 | BaseMethodDeclarationOrchestrator.cs, AccessorSyntaxOrchestrator.cs, AnonymousFunctionExpressionOrchestrator.cs, ExpressionBodiedPropertyOrchestrator.cs, LocalFunctionStatementOrchestrator.cs, ConditionalExpressionOrchestrator.cs, StaticFieldDeclarationOrchestrator.cs, ConstantPatternSyntaxOrchestrator.cs, DoNotMutateOrchestrator.cs, SyntaxNodeOrchestrator.cs, OrchestrationHelpers.cs, MutatorClassNameAliases.cs, INodeOrchestrator.cs, BaseFunctionOrchestrator.cs:100–145 (Re-Read), StringMutator.cs (Re-Read für G-14) | ✅ gelesen — CsharpNodeOrchestrators/ damit KOMPLETT (26/26) |
| 6 | Mutants/Filters/ komplett (7/7): EquivalentMutantFilterPipeline.cs, IEquivalentMutantFilter.cs, IdentityArithmeticFilter.cs, IdempotentBooleanFilter.cs, ConservativeDefaultsEqualityFilter.cs, RoslynDiagnosticsEquivalenceFilter.cs, RoslynSemanticDiagnosticsEquivalenceFilter.cs | ✅ gelesen |
| 7 | Instrumentation/ komplett (8/8: +BaseEngine, IInstrumentCode, StaticInstrumentationEngine, StaticInitializerMarkerEngine, DefaultInitializationEngine); InjectedHelpers/ komplett (3/3: MutantContext, MutantControl, CodeInjection); Compiling/ komplett (6/6: CSharpRollbackProcess, CsharpCompilingProcess, 2 Records, 2 Interfaces) | ✅ gelesen |
| 8 | MutationTest/ komplett (7/7: CsharpMutationProcess, MutationTestProcess, MutationTestExecutor, MutationTestInput, 3 Interfaces — Executor/DTOs befundfrei); CoverageAnalysis/ komplett (2/2: CoverageAnalyser, ICoverageAnalyser) | ✅ gelesen |
| 9 | MutantFilters/ komplett (13/13: Factory, MutantFilter-Enum, IMutantFilter, Broadcast, IgnoredMethod, IgnoreMutation, IgnoreBlock, ExcludeFromCodeCoverage, ExcludeLinq, FilePattern, Since, Baseline, GeneratedCodeFilterExtension); root komplett (3/3: StrykerRunner, StrykerRunResult, IStrykerRunner — letztere zwei befundfrei) | ✅ gelesen |
| 10 | ProjectComponents/ komplett (12/12: ProjectComponent, FolderComposite, IProjectComponentsExtensions, CsharpFileLeaf, CsharpFolderComposite, Solution, SolutionInfo, SourceProjectInfo, TestProjectsInfo, TestProject, TestFile, TestCase) — **damit Scope KOMPLETT: 96/96 Dateien gelesen** | ✅ gelesen |

## Findings

| # | Status | Sev | Datei | Kurzbefund |
|---|--------|-----|-------|------------|
| **G-01** | **BESTÄTIGT (Mechanik komplett)** | **P2** | Instrumentation/EndingReturnEngine.cs:17–26 + CsharpNodeOrchestrators/BaseFunctionOrchestrator.cs:140 | **F-14-Auflösung:** AddEndingReturn würde den CS0161-Schaden des if/else-Wraps heilen (nach Wrap ist `Statements.Last()` ein IfStatement, nicht mehr Return → Early-Exit Zeile 19 griffe nicht; Zeile 23 fände das Return im else-Zweig) — aber der Mutations-Pfad ruft die Engine NIE auf: Zeile 140 wendet nur `InjectOutParametersInitialization` auf das Inject-Ergebnis an; AddEndingReturn existiert ausschließlich im No-Mutations-Pfad (Zeile 130–135). Fix-Ort eindeutig: `SwitchToThisBodies(targetNode, MutantPlacer.AddEndingReturn(newBody, returnType), null)` im Mutations-Pfad. Erklärt Block-CS0161 (Probe 2/2) und vermutlich Reporter-D-CS0161-Anteil |
| G-02 | NOTIZ | P3 | MutationContext.cs | Sauber geschichtetes Kontext-Design (Stack-basierte MutationStore, Block/Member-Kind-Kontexte, Comment-Filter-Vererbung) — keine Auffälligkeit im Kontext-Handling selbst |
| **G-03** | **BESTÄTIGT (Mechanik komplett)** | **P2** | CsharpNodeOrchestrators/BaseFunctionOrchestrator.cs:140 + Helpers/RoslynHelper.cs:167 | **F-10-Auflösung:** Deklarations-Level-Mutationen (MethodBodyReplacement, GenericConstraint×2) haben KEINEN Hosting-Pfad — MutationControl endet bei `Member` („no higher supported structure"), MutantPlacer kennt nur Expression-/Statement-Wraps. Konkreter Ablauf bei block-bodied Methoden: Member-Store hat Pending → Mutations-Pfad ruft `context.InjectMutations(block, GetBodies(sourceNode).expression!, …)` — **`expression!` ist für Block-Bodies eine null-forgivene NULL** → ADR-028-Guard fängt sie und etikettiert: „sourceNode is null — **orchestrator bug**, mutation cannot be injected" → CompileError. Die Pipeline benennt den eigenen Bug in der Diagnose, versteckt ihn aber in der CE-Statistik. Konsequenz: 2,5 All/Stronger-Mutatoren sind systematisch DOA (Probe 173: MethodBodyReplacement 1/1 CE ✓ konsistent). **Eleganteste Fix-Richtung:** Emissions-Form der Mutatoren ändern — `OriginalNode=Body, ReplacementNode=newBody` statt ganzer Deklaration → hostet als Block-Level via IfEngine, null Engine-Arbeit (Kandidat für #279-Checkliste) |
| G-04 | NOTIZ | P3 | MutationStore.cs:236 (ApplyMutationsValidated) + Leave():73 | ADR-028-Validierungsschicht ist solide (Slot-Reject → korrekt etikettierter CE statt Visitor-Crash); Drop-Pfad in Leave() („Could not be injected") nur DBG-geloggt — un-hostbare Mutationen verschwinden für User unsichtbar in der CE-Statistik. Verbesserung: WARN-Level + eigener Mutant-Status (z. B. `NotSupported`) statt CompileError-Vermischung |
| G-05 | BESTÄTIGT | P3 | CsharpMutantOrchestrator.cs:254/292 | Log-Lüge im Duplikat-Pfad: `newMutant.Id` wird erst NACH dem Dedup-Check vergeben (Zeile 254) → `LogMutantDuplicate` (Zeile 292) loggt für den verworfenen Mutanten immer `0` („Mutant 0 discarded as duplicate of 37"). Debug-only, aber irreführend bei CE-/Dedup-Forensik |
| G-06 | VERDACHT | P3 | CsharpMutantOrchestrator.cs:223 | `mutator.Mutate(current, semanticModel, Options!)` — Options ist per Basisklassen-Vertrag nullable (`IStrykerOptions?`), wird aber null-forgiven durchgereicht. Mutatoren, die `options.MutationLevel` o. ä. lesen, würden mit NRE crashen — und wegen fehlendem try/catch (#277) den gesamten Lauf reißen. Produktiv immer gesetzt; Risiko = Test-/Embedding-Szenarien. Gehört in die #277-Robustheits-Systematik |
| G-07 | VERDACHT (RollbackProcess prüfen) | P3 | MutantPlacer.cs:141–154 | `RemoveMutant` nimmt `GetAnnotatedNodes(Injector).FirstOrDefault()` = self-ODER-Descendant in Dokumentordnung. Wird die Methode mit einem nicht-annotierten Knoten aufgerufen, entfernt sie still die ERSTE Kind-Injection statt zu failen (Remark verspricht „keep any child one"). Vertrag hängt an Disziplin der Aufrufer (RollbackProcess) — beim Compiling/-Block verifizieren |
| G-08 | NOTIZ | P3 | MutantPlacer.cs:48/149/213 | Statische Engine-Registry: Lock beim Registrieren, lock-freies Lesen in `RemoveMutant`; Instanz-Lazy-Init `GetBinaryExpression` nicht thread-safe. Praktisch benign (Registrierung in statischen Initialisierern, Parse idempotent) — nur Architektur-Notiz |
| G-09 | NOTIZ (aufgelöst in Batch 8) | P3 | Mutant.cs:45 | `resultRanTests.IsEveryTest \|\| !resultRanTests.IsEveryTest && AssessingTests.IsIncludedIn(...)` ≡ `A \|\| B` — redundante Bedingung (Upstream-Erbe). Pending-Frage GEKLÄRT: MutationTestExecutor re-testet Pending-Reste (forceSingle-Kaskade bei Session-Timeout), bei inkonklusiven Läufen ERROR-Log; MutationTestProcess.OnMutantsTested WARNt „Mutation {Id} was not fully tested" und skippt Reporting. By design mit Sicherheitsnetz — kein Bug |
| **G-10** | **VERDACHT (hoch; Probe in Verifikationsphase)** | **P2** | Helpers/RoslynHelper.cs:36–38 (ContainsDeclarations) + ExpressionSpecificOrchestrator.cs:22 + Orchestrator-Liste (generischer `NodeSpecificOrchestrator<PatternSyntax,PatternSyntax>`) | **Schwester-Lücke zu #278, anderer Mechanismus (Wrap-Level-Fehlklassifikation statt Pattern-Negation), Default-Profil-Reichweite.** Zwei Schichten: (1) `ContainsDeclarations` prüft nur `DeclarationExpression` + `DeclarationPattern` — **VarPattern (`x is var v`), RecursivePattern-Designation (`x is { Length: > 0 } s`), ListPattern-Designation (`x is [..] all`) fehlen** → solche Expressions entgehen dem Block-Lift, eigene Mutationen landen im Ternary-Wrap. (2) Pattern-INTERNE Mutationen (RelationalPatternMutator `> 0`→`>= 0`, BinaryPatternMutator — beide Basic/Defaults!) werden vom generischen PatternSyntax-Orchestrator per Default-`AddMutations` **ohne jeden Designation-Check** auf Expression-Level gespeichert → injiziert beim umschließenden Is-Expression → Ternary `(active ? x is {Length: >= 0} s : x is {Length: > 0} s)` dupliziert die Designation im selben Statement-Scope → CS0128; bei Use-after alternativ CS0165. Probe-Kandidat: `obj is { Length: > 2 } s ? s : "x"`. Fix-Richtung: Designation-aware Store-Level (Pattern-Orchestrator: Block wenn enclosing IsPattern eine `SingleVariableDesignationSyntax` enthält) + `ContainsDeclarations` um VarPattern/Designations erweitern |
| G-11 | NOTIZ | P3 | LocalDeclarationOrchestrator.cs:8 | Doc-Kommentar lügt: „Handle const declarations" — const-Deklarationen werden bereits in der Orchestrator-Liste per `DoNotMutateOrchestrator` (`t.IsConst`) gefiltert; diese Klasse promotet stattdessen ALLE Local-Declaration-Mutationen auf Block-Level (InjectMutations = no-op). Reines Doku-Erbe |
| G-12 | NOTIZ | P3 | CommentParser.cs:192–204 | **In-Repo-Präzedenzfall für #277:** `ProcessComment` fängt `RegexMatchTimeoutException` → WARN-Log mit Position + weiter (Kommentar wird ignoriert, Lauf überlebt). Exakt dieses Muster fehlt um `mutator.Mutate` (CsharpMutantOrchestrator:223). Nebenbefunde: Pattern-Regex ankert `^` nur auf die Single-Line- und `$` nur auf die Multi-Line-Alternative (praktisch benign, da Trivia-Text ohne Padding beginnt); `\s*` zwischen mode und scope optional → „disableonce" parst als disable+once (kosmetisch) |
| G-13 | BESTÄTIGT | P3 | ExpressionBodiedPropertyOrchestrator.cs:47 vs. StaticFieldDeclarationOrchestrator.cs:22 / StaticConstructorOrchestrator.cs:24 | Static-Marker-Asymmetrie: Feld-Initializer und Static-Ctor gaten `PlaceStaticContextMarker` auf `context.MustInjectCoverageLogic`; statische Property-Initializer platzieren den Marker BEDINGUNGSLOS (MutationContext/Placer prüfen intern nichts — verifiziert). Folge: unnötige Marker-Instrumentation im User-Assembly, wenn Coverage-Logik aus ist. Kein Korrektheitsbruch, aber Inkonsistenz mit Laufzeit-Overhead |
| **G-14** | **VERDACHT (hoch; Probe in Verifikationsphase)** | **P2** | StringMutator.cs:18–35 (+ InlineConstantsMutator/BooleanMutator analog) + Orchestrator-Liste (kein CaseSwitchLabel-Schutz) | **Konstanten-Kontext Nr. 5 ungeschützt: `case`-Labels.** Die DoNotMutate-Liste deckt Attribute, Parameter-Defaults, Enum-Member, const-Felder/-Locals; Konstanten-Patterns deckt `ConstantPatternSyntaxOrchestrator.BlockInjection()`. Legacy-`case "abc":` (CaseSwitchLabelSyntax.Value) und `goto case`-Expressions haben NICHTS davon: StringMutator (Defaults!, einzige Guards = Regex/Guid-ctor via Parent³ [F-20] + IsAStringExpression) mutiert das Label-Literal → Ternary-Wrap im case-Label → CS0150 „constant expected" → garantierter CE. Reichweite: jedes `switch` über string-Konstanten im DEFAULT-Profil; numerische/bool-Labels analog ab Stronger (InlineConstants/Boolean). Probe-Kandidat: `switch(s){case "a": return 1;}`. Fix-Richtung: `DoNotMutateOrchestrator<CaseSwitchLabelSyntax>` (+ GotoStatement mit CaseKeyword) — deckt alle Mutatoren zentral statt Guard je Mutator |
| **G-15** | **BESTÄTIGT (Mechanik über alle 3 Schichten verifiziert; Trigger-Häufigkeit offen)** | **P2** | CsharpNodeOrchestrators/OrchestrationHelpers.cs:81–98 → CsharpMutationProcess.CompileMutations:140–154 → CoverageAnalyser:144–148 | **ADR-032-Sicherheitsnetz erzeugt Geister-Mutanten → False Survivors.** Kette komplett verifiziert: (1) Per-Child-Reject/Bulk-Fallback verwirft bereits gewrappte Subtrees — enthaltene Mutanten sind registriert + aus dem Store konsumiert; (2) `CompileMutations` markiert NUR `RollbackedIds` als CompileError — Geister erzeugen nie einen Compile-Fehler; (3) Coverage-Modus: Geist-ID nie in `RegisterCoverage` → `CoveringTests.IsEmpty` → **NoCoverage „Not covered by any test"**; ohne Coverage: AssumeAllTestsAreNeeded → getestet → Assembly verhält sich wie Baseline → **Survived**. Beides leise Score-Verfälschung; der XML-Doc-Kommentar („reporters can classify as CompileError") ist aspirational — es gibt nur DBG-Logs. Multi-Mutant-Verlust pro Drop möglich. Fix-Richtung: Drop-Pfad muss betroffene Mutant-IDs sammeln und als CompileError/NotSupported markieren (Plumbing: OrchestrationHelpers braucht Zugriff auf die Mutanten des verworfenen Wraps — via MutantPlacer-Annotationen des Subtrees auslesbar) |
| G-16 | NOTIZ | P3 | BaseFunctionOrchestrator.cs:128+135 | No-Mutations-Pfad: `wasInExpressionForm` ist an Zeile 128 IMMER false (Expression-Form-Targets returnen schon bei Zeile 119–122, Kinder-Orchestrierung konvertiert nie zu Block) → toter Guard; Zeile 135 ruft `AddEndingReturn` ein ZWEITES Mal auf (idempotent, da Block dann mit Return endet — redundant, nicht falsch). Für den G-01-Fix relevant: diese Region wird ohnehin angefasst |
| **G-17** | **BESTÄTIGT (Mechanik komplett)** | **P2** | Mutants/Filters/ (alle 5 Default-Filter) + CsharpMutantOrchestrator.cs:241 | **F-25-Auflösung: KEIN Filter fängt generische No-op-Emissionen** (Replacement ≡ Original, z. B. TypeDrivenReturn `return 0;`→`return 0;`, ConstantReplacement auf bereits-Zielwert). IdentityArithmeticFilter ist strukturell ein No-op-Detektor (`orig.IsEquivalentTo(repl)`!), aber auf x+0/x±0/x*1-Shapes beschränkt; ConservativeDefaults nur eq→ordered-unsigned-zero; IsMutantDuplicate dedupt nur gegen ANDERE Mutanten, nie gegen das Original. Folge: No-op-Mutanten = garantierte False Survivors (Score-Drückung). Reichweite primär Stronger/All (TypeDrivenReturn/ConstantReplacement); Fix trivial: generischer Filter #0 `SyntaxFactory.AreEquivalent(OriginalNode, ReplacementNode)` — O(tree-compare), fängt ALLE Shapes |
| **G-18** | **BESTÄTIGT (Mechanik komplett)** | **P3 (konstruktiv für #279)** | RoslynSemanticDiagnosticsEquivalenceFilter.cs | **F-08-Auflösung:** Pipeline sauber erweiterbar (Interface + statische Liste + any-match; Präzedenzen v2.1/v2.4/v3.2.9). ABER: Speculative-Binding-Technik hat harte Decke — spekulative Modelle werfen `NotSupportedException` auf `GetDiagnostics()` (im Code dokumentiert!), Symbol-Binding (`CandidateReason != None`) sieht nur Resolution-Fehler. **Operator-Typfehler (CS0019), readonly (CS1656/CS0200), Flow (CS0161/CS0165/CS4016) bleiben strukturell unerreichbar** auf diesem Weg — Flow braucht Voll-Compilation (= RollbackProcess-Domäne). Konkrete Erweiterungs-Hypothese für die CS0019-Klasse: `GetSpeculativeTypeInfo(...).Type is IErrorTypeSymbol` (Operator-Resolution-Failure liefert Error-Type) — billig, Probe-Kandidat für Sprint 178/#279 |
| G-19 | NOTIZ | P3 | EquivalentMutantFilterPipeline-Nutzung (CsharpMutantOrchestrator:241–246) | Transparenz-Lücke: equivalence-gefilterte Mutationen werden per `continue` verworfen, BEVOR ein Mutant entsteht — kein `Ignored`-Status, kein Report-Eintrag, nur DBG-Log. Filter-False-Positives (z. B. Statement-Pfad: Ambiguous-Binding in verändertem Kontext → „equivalent") = unsichtbarer Mutanten-Verlust. Upstream zeigt Ignored+Reason im Report. Verbesserung: Ignored-Mutant mit `ResultStatusReason = "equivalent: <FilterId>"` statt continue |
| G-20 | NOTIZ | P3 | RoslynSemanticDiagnosticsEquivalenceFilter.cs:142–163 | Statement-Pfad iteriert ALLE Expression-Descendants mit je `GetSymbolInfo` + try/catch — O(Statement-Größe) je Statement-Mutation; bei BlockMutator auf großen Methoden = O(Block) je Block-Mutant. Nur Mutationsphase, kein Hot-Path — Architektur-Notiz, ggf. BenchmarkDotNet-Kandidat |
| **G-21** | **BESTÄTIGT (Mechanik; Probe in Verifikationsphase)** | **P2** | StaticInitializerMarkerEngine.cs:24–28 + ExpressionBodiedPropertyOrchestrator.cs:47 (G-13) + MutantContext.TrackValue<T> + CSharpRollbackProcess (Heilungs-Pfad) | **TrackValue-Lambda-Wrap löscht Konstanten-/Target-Typing-Semantik von static Initializern.** `TrackValue<T>(Func<T>)` (generisch, verifiziert) um Initializer gewickelt: `static byte B = 5;` → T=int → CS0266; `= new();`/`new([...])` → kein natural type → CS8754/CS0411; Enum `= 0`, `null`-/`default`-Literale analog. Static-Property-Initializer (G-13: UNGATED) triggern in JEDEM Modus, Felder im Coverage-all-Modus. **In-Repo-Trigger: EquivalentMutantFilterPipeline.cs:31 (`Default { get; } = new([...])`).** Warum Dogfood trotzdem grün: Rollback identifiziert den Marker-Knoten via Injector-Annotation (Id=−1, nie null — G-28), `RemoveMutant`→`Revert` extrahiert den Original-Initializer → Heilung in Extra-Compile-Runde, auf dem Identify-Pfad NUR DBG-Logs → **still**. Netto-Schaden: verschwendete Compile-Runde(n) pro betroffenem Projekt + stiller Marker-Verlust (Coverage-all: Static-Context-Tracking unvollständig → Isolations-Entscheidungen potenziell falsch). Fix-Richtung: Marker-Skip für nicht-wrapbare Initializer-Shapes (ImplicitObjectCreation, numerische Literale mit Konversions-Bedarf, null/default) analog zum existierenden InitializerExpression-Skip; Property-Pfad auf MustInjectCoverageLogic gaten (G-13) |
| **G-22** | **BESTÄTIGT** | **P2 (Perf, User-Prozess)** | InjectedHelpers/MutantControl.cs:225–238 | `RegisterCoverage` macht je Mutationspunkt-AUSFÜHRUNG `lock` + `List.Contains` (O(n)) ×2 — im Coverage-Capture-Lauf wird `IsActive` an JEDEM instrumentierten Punkt aufgerufen: heiße Schleifen über großen Projekten zahlen O(#covered) pro Hit (quadratisches Verhalten, im USER-Testprozess). `HashSet<int>` wäre drop-in (Helper ist C#-2-beschränkt — HashSet ist .NET 3.5; Alternativ `Dictionary<int,bool>` für C#2-Treue). BenchmarkDotNet-Kandidat |
| G-23 | VERDACHT (Sprint-176-Schwerpunkt vormerken) | P2-Kandidat | InjectedHelpers/MutantControl.cs:97–109 | MTP-File-Control: Re-Read nur bei `LastWriteTimeUtc.Ticks`-Änderung — FS-Timestamp-Granularität (NTFS ~ms) ⇒ zwei schnelle Mutant-Wechsel innerhalb derselben Tick-Auflösung → zweiter Write wird NICHT gelesen → **stale ActiveMutant, Tests laufen gegen falschen Mutanten**. Prozess-Reuse-Runner wechselt Mutanten schnell. Gehört zur #274-Test-Runner-Kette (Analyse D) |
| G-24 | NOTIZ | P3 | InjectedHelpers/MutantControl.cs:167–171 | `CurrentDomain_ProcessExit(object, EventArgs)` ist toter Code — der statische Ctor registriert ein ANDERES anonymes Delegate (FlushCoverageToFile); die GC.KeepAlive-Methode wird nie referenziert. Upstream-Erbe |
| **G-25** | **VERDACHT (hoch)** | **P2 (#277-Klasse)** | Instrumentation/DefaultInitializationEngine.cs:57–59 | `p.Type!.BuildDefaultExpression()` — null-forgiven. **C# 14 „simple lambda parameters with modifiers" erlaubt `(out x) => …` OHNE Typ** → `p.Type == null` → NRE zur MUTATIONSZEIT → reißt ohne #277-try/catch den ganzen Lauf. Trigger: jede Lambda mit typlosem out-Parameter im Mutationsziel (Projekt zielt auf C# 14!). Probe-Kandidat: `M((out x) => { x = 1; });` |
| G-26 | NOTIZ | P3 | Instrumentation/StaticInstrumentationEngine.cs:23–31 | `Revert` returnt bei Shape-Mismatch STILL den instrumentierten Knoten (kein Throw) — Asymmetrie zu StaticInitializerMarkerEngine/BaseEngine (werfen InvalidOperationException). Bei jemals abweichendem Shape würde Rollback die Instrumentation nicht entfernen und ohne Fehler weiterlaufen (CE bliebe) — latent, da Shape by-construction stabil |
| G-27 | NOTIZ | P3 | InjectedHelpers/CodeInjection.cs | Solide: case-sensitives `Replace("Stryker", …)` verschont ENV-Konstanten (`STRYKER_…` upper-case) und ersetzt Namespace+Klassenbezüge korrekt; Selector-Extraktion VOR Replace. Rest-Notizen: `new Random()`-Namespace = nicht-deterministisches Assembly je Lauf (Baseline-neutral, da Mutant-zentriert); `GetSourceFromResource` `stream!` → NRE statt klarer Fehlermeldung bei fehlender Embedded-Resource (Startup-Assert) |
| G-28 | NOTIZ | P3 | MutantPlacer.FindAnnotations:178 + CSharpRollbackProcess.ExtractMutationInfo:128–140 | `MutantInfo.Id`-Semantik: FindAnnotations liefert für Engine-annotierte Knoten ohne MutationId **−1, nie null** → `info.Id != null` bedeutet faktisch „hat Engine-Annotation". Alle RollBackedIds-Guards konsistent (`>= 0`/`!= -1`) ✓; `info.Id ?? -1` (Zeile 137) ist toter Coalesce. **Diese −1-Mechanik ist zugleich der G-21-Heilungspfad** (Marker-Knoten werden als „Mutation" rollbackfähig). Fragil, aber funktional — Doku-würdig |
| G-29 | NOTIZ | P3 | CsharpCompilingProcess.GetCSharpCompilation + CSharpRollbackProcess.Start:36–48 | Generator-Trees: Rollback-Dictionary wird aus der POST-Generator-Compilation gebaut → Diagnostics in generierten Trees finden ihren Eintrag (kein KeyNotFound-Crash ✓). ABER: Fehler IN generierten Trees sind nicht rollbackfähig (keine Annotationen) → RemoveCompileErrorMutations findet nichts → same-tree-Throw („Internal error") oder Leerlauf bis MaxAttempt. Generatoren laufen auf MUTIERTEN Trees (Design-Notiz — Syntax-inspizierende Generatoren sehen Wraps) |
| G-30 | NOTIZ | P3 | CSharpRollbackProcess.ScanForSuspiciousMutations:239–251 + DisplayName:322 | Block-first-Heuristik: existiert IRGENDEIN Block-Mutant im umschließenden Member, werden NUR Block-Mutanten entfernt — war ein anderer Mutant ursächlich, kostet das eine weitere Runde (gedeckelt durch MaxAttempt=50, je Runde Voll-Emit). Plus kosmetisch: `DisplayName` für Accessors druckt `"{Keyword} {Keyword}"` — Safe-Mode-Meldung zeigt „get get" |
| **G-31** | **VERDACHT (hoch)** | **P2 (Hang-Klasse)** | CsharpCompilingProcess.TryCompilation:181–215 + ScanForCauseOfException | `while (emitResult == null)` ist UNBEGRENZT: Roslyn-Emit-NRE → ScanForCauseOfException emittiert jede Datei einzeln und bereinigt nur Dateien, die SOLO crashen. Reproduziert sich der NRE nur in Interaktion (kein Einzeldatei-Crash), kommt die Compilation UNVERÄNDERT zurück → Endlosschleife, pro Iteration O(Dateien) Voll-Emits + ein Gesamt-Emit. Fix-Richtung: Schleifenzähler/Abbruch nach N Scan-Runden ohne Tree-Änderung |
| G-32 | NOTIZ | P3 | CoverageAnalyser.cs:56–60 + 42/27 | Toter Code: `if (!coverage.Any())` ist unerreichbar — leeres coverage hätte bei Zeile 42 (`Sum == 0`) bereits returnt. Dazu Mehrfach-Enumeration von `coverage` (Sum, Aggregate, Any) — falls `runner.CaptureCoverage` je lazy liefert, würde der Capture mehrfach laufen (in Analyse D/Sprint 176 gegen Runner-Implementierung prüfen). Positiv-Notiz: `IsStaticValue` aus Generierungszeit (EnterStatic) bleibt im Static-Pfad (Zeile 129) wirksam, auch wenn der Laufzeit-Marker fehlt — **mildert G-21** (Marker-Verlust kostet NeedEarlyActivation-/CoveredOutsideTest-Präzision, nicht die Static-Erkennung selbst) |
| G-33 | BESTÄTIGT (Mechanik) | P3 | SinceMutantFilter.cs:119–143 + StrykerRunner.cs:96/101 | Changed-Tests-Reaktivierung hängt an `mutant.CoveringTests` — die füllt erst `AnalyzeCoverage`. Reihenfolge im Runner: Coverage (Z. 96) VOR Filtern (Z. 101) → Default-Modus OK. Bei `--coverage-analysis off` + `--since`: CoveringTests bleibt `NoTest()` → `IsEmpty → continue` für JEDEN Mutanten → Test-Änderungen reaktivieren NICHTS, Mutanten bleiben „not changed"-Ignored → **Under-Testing in dieser Options-Kombination**. Fix-Richtung: bei fehlender Coverage-Info konservativ ALLE Mutanten der von Test-Änderungen betroffenen Projekte re-enablen |
| G-34 | BESTÄTIGT | P3 | ExcludeLinqExpressionFilter.cs:42 | `expressionString.EndsWith(Enum.GetName(r))` — Suffix-Matching: `--ignore Linq:Count` ignoriert auch `LongCount`-Mutanten (jedes *Count-Linq-Ziel); `Sum`-Exclude trifft jede auf „Sum" endende Methodenkette. Over-Ignore = Under-Testing. Fix: exaktes Segment-Matching (`.Name == enumName`) statt EndsWith |
| G-35 | NOTIZ | P3 | IgnoredMethodMutantFilter.cs:47–49 | `ImplicitObjectCreationExpressionSyntax`-Zweig matcht nur unter `VariableDeclarationSyntax` (Locals + Felder ✓) — Property-Initializer `{ get; } = new(...)`, `return new(...)`, Argument-Positionen `M(new(...))` fallen durch → ignored-methods `*.ctor`-Config greift dort still nicht. Konsistenz-Lücke zur expliziten ObjectCreation |
| G-36 | NOTIZ | P3 | MutantFilterFactory.cs:33–63 + BroadcastMutantFilter.cs:20–35 | `SortedSet<IMutantFilter>` mit Type-Comparer: Compare==0 ⇒ SortedSet verwirft den zweiten Filter STILL — Footgun für künftige gleichtypige Filter. Statische mutable Factory-Felder (Single-Run-Annahme). Broadcast: `remainingMutantsToTest` bleibt je Stufe lazy → LINQ-Ketten stapeln sich über Filterstufen (Mehrfach-Evaluation; bei 5–8 Filtern harmlos) |
| G-37 | NOTIZ | P3 | BaselineMutantFilter.cs:44 + 107 | (a) `GetBaselineAsync().Result` — sync-over-async im Ctor (CLI ohne SyncContext = kein Deadlock, aber Anti-Pattern gegen Projekt-Standard ConfigureAwait-Disziplin); (b) `Enum.Parse<MutantStatus>(baselineMutant.Status)` ungeguardet auf EXTERNEM Input (Dashboard-/Fremdversions-Baseline) → ArgumentException reißt den Lauf (#277-Robustheitsklasse). Fix: TryParse + Skip-on-unknown |
| G-38 | NOTIZ | P3 | Solution.cs:21 vs. FolderComposite.cs:30–34 | `Solution.Add` setzt `child.Parent` NICHT (FolderComposite.Add schon) — Projekt-Roots unter einer Solution behalten Parent=null. Aktuell folgenlos (`IsComponentExcluded`s `Parent?.Parent == null`-Heuristik liefert für beide Fälle „nicht ausgeschlossen"), aber eine latente Konsistenz-Falle für künftige Parent-Walks |
| G-39 | NOTIZ | P3 | TestProjects/TestProject.cs:38 + ProjectComponents allgemein | Test-Dateien werden mit `encoding: Encoding.UTF32` geparst (Metadata-Encoding für Checksums — Dateien sind real UTF-8; Upstream-Erbe, praktisch folgenlos da Test-Trees nie kompiliert werden). Rest des Blocks befundfrei: Score-NaN-Pfad sauber (0/0→NaN→Health.None), Backup-Once/Restore-Copy-Semantik korrekt (alte Backups = wahres Original nach Crash-Läufen), TestFile/TestCase-Equality konsistent |

## Detail-Einträge
