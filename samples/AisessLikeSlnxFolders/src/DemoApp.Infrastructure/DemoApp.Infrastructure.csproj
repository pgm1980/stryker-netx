<Project Sdk="Microsoft.NET.Sdk">

  <!--
    DemoApp.Infrastructure — Sprint 159 Fix-4 integration-test fixture.
    Infrastructure layer: references Domain only. Contains Repository with
    in-memory storage logic that produces additional mutation targets.
  -->

  <PropertyGroup>
    <AssemblyName>DemoApp.Infrastructure</AssemblyName>
    <RootNamespace>DemoApp.Infrastructure</RootNamespace>
    <IsPackable>false</IsPackable>
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\DemoApp.Domain\DemoApp.Domain.csproj" />
  </ItemGroup>

</Project>
